<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Search Products - Daraz Seller</title>
  <link rel="stylesheet" href="styles.css">
  <style>
    body {
      margin: 0;
      padding: 0;
      background: #f5f5f5;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }

    .search-hero {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 60px 20px;
      text-align: center;
      color: white;
    }

    .search-hero h1 {
      margin: 0 0 20px 0;
      font-size: 40px;
      font-weight: bold;
    }

    .search-hero p {
      margin: 0 0 30px 0;
      font-size: 16px;
      opacity: 0.9;
    }

    .search-container {
      max-width: 700px;
      margin: 0 auto;
      padding: 20px;
    }

    .search-bar {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
    }

    .search-input {
      flex: 1;
      padding: 15px 20px;
      border: 2px solid #ddd;
      border-radius: 8px;
      font-size: 16px;
      transition: all 0.3s;
    }

    .search-input:focus {
      outline: none;
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    .search-btn {
      padding: 15px 30px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .search-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
    }

    .search-btn:active {
      transform: translateY(0);
    }

    .search-tabs {
      display: flex;
      gap: 10px;
      margin-bottom: 30px;
      border-bottom: 2px solid #e0e0e0;
    }

    .search-tab-btn {
      padding: 12px 20px;
      background: transparent;
      border: none;
      border-bottom: 3px solid transparent;
      cursor: pointer;
      font-weight: 600;
      color: #666;
      transition: all 0.3s;
      font-size: 14px;
    }

    .search-tab-btn.active {
      color: #667eea;
      border-bottom-color: #667eea;
    }

    .search-tab-btn:hover {
      color: #667eea;
    }

    .search-results {
      background: white;
      border-radius: 8px;
      padding: 30px;
      margin-bottom: 20px;
      min-height: 200px;
      display: none;
    }

    .search-results.show {
      display: block;
    }

    .result-item {
      padding: 15px 0;
      border-bottom: 1px solid #eee;
    }

    .result-item:last-child {
      border-bottom: none;
    }

    .result-title {
      font-weight: 600;
      color: #333;
      margin-bottom: 5px;
      font-size: 16px;
    }

    .result-url {
      color: #667eea;
      font-size: 14px;
      margin-bottom: 5px;
      word-break: break-all;
    }

    .result-desc {
      color: #666;
      font-size: 14px;
      line-height: 1.5;
    }

    .no-results {
      text-align: center;
      padding: 40px 20px;
      color: #999;
    }

    .no-results-icon {
      font-size: 48px;
      margin-bottom: 15px;
    }

    .url-input-section {
      background: white;
      border-radius: 8px;
      padding: 30px;
      margin-top: 30px;
    }

    .url-input-section h2 {
      margin-top: 0;
      color: #333;
      font-size: 18px;
    }

    .url-form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
    }

    .form-group label {
      font-weight: 600;
      margin-bottom: 8px;
      color: #333;
      font-size: 14px;
    }

    .form-group input,
    .form-group textarea {
      padding: 10px 15px;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 14px;
      font-family: inherit;
      transition: border-color 0.3s;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: #667eea;
    }

    .form-group textarea {
      resize: vertical;
      min-height: 100px;
    }

    .submit-btn {
      padding: 12px 24px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 6px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .submit-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
    }

    .success-message {
      background: #d4edda;
      color: #155724;
      padding: 12px;
      border-radius: 6px;
      margin-bottom: 15px;
      display: none;
    }

    .success-message.show {
      display: block;
    }

    .loading {
      text-align: center;
      padding: 20px;
      color: #667eea;
    }

    .loading-spinner {
      display: inline-block;
      width: 20px;
      height: 20px;
      border: 3px solid #ddd;
      border-top-color: #667eea;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    @media (max-width: 600px) {
      .search-hero h1 {
        font-size: 28px;
      }

      .search-bar {
        flex-direction: column;
      }

      .search-input,
      .search-btn {
        width: 100%;
      }

      .search-container {
        padding: 10px;
      }
    }
  </style>
</head>
<body>
  <header class="top-bar">
    <div class="logo" onclick="goHome()">🛍️ Daraz Seller</div>
    <nav class="nav-links">
      <a href="index.html">Home</a>
      <a href="search.html" class="active">Search</a>
      <a href="seller-center.html">Seller</a>
      <a href="contact.html">Contact</a>
    </nav>
  </header>

  <div class="search-hero">
    <h1>🔍 Product Search & URL Lookup</h1>
    <p>Find products, scrape URLs, and discover details from competitor sites</p>
  </div>

  <div class="search-container">
    <!-- Product Search Section -->
    <div class="search-tabs">
      <button class="search-tab-btn active" onclick="switchSearchTab('product')">📦 Search Products</button>
      <button class="search-tab-btn" onclick="switchSearchTab('url')">🔗 Lookup URL</button>
    </div>

    <!-- Product Search Tab -->
    <div id="product-tab">
      <div class="search-bar">
        <input 
          type="text" 
          id="product-search" 
          class="search-input" 
          placeholder="Enter product name or keyword..."
          onkeypress="handleSearchEnter(event)"
        >
        <button class="search-btn" onclick="searchProducts()">Search 🔍</button>
      </div>

      <div class="search-results" id="product-results">
        <div id="results-content"></div>
      </div>
    </div>

    <!-- URL Lookup Tab -->
    <div id="url-tab" style="display: none;">
      <div class="url-input-section">
        <h2>🔗 Enter Product URL</h2>
        <p style="color: #666; font-size: 14px;">Submit a product URL to scrape details from eBay, AliExpress, or other sites</p>
        
        <form class="url-form" onsubmit="handleURLSubmit(event)">
          <div class="success-message" id="url-success"></div>

          <div class="form-group">
            <label for="product-url">📱 Product URL</label>
            <input 
              type="url" 
              id="product-url" 
              placeholder="https://www.ebay.com/itm/..." 
              required
            >
            <small style="color: #999; margin-top: 5px;">Supported: eBay, AliExpress, Daraz.lk, Amazon</small>
          </div>

          <div class="form-group">
            <label for="product-title">📝 Product Title (optional)</label>
            <input 
              type="text" 
              id="product-title" 
              placeholder="e.g., Wireless Earbuds Pro"
            >
          </div>

          <div class="form-group">
            <label for="product-notes">📋 Notes (optional)</label>
            <textarea 
              id="product-notes" 
              placeholder="Add any notes about this product..."
            ></textarea>
          </div>

          <button type="submit" class="submit-btn">📤 Submit URL</button>
        </form>
      </div>
    </div>
  </div>

  <script src="search-script.js"></script>
</body>
</html>
